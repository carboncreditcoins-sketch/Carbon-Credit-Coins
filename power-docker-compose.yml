version: "3"

services:
  backend:
    build: ./backend
    ports:
      - "3001:3001"

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"

  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
